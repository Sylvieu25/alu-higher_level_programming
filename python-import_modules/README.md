# Python Import Modules

This directory demonstrates Python module import and function usage.

Files:
- **add_0.py**: Defines the addition function `add(a, b)`.
- **0-add.py**: Imports `add` from `add_0.py` and prints `1 + 2 = 3`.
- **0-import_add.py**: Safely imports `0-add.py`.
